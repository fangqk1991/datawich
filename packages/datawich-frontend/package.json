{
  "name": "datawich-frontend",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "webpack-dev-server --config webpack.dev.js",
    "build": "rm -rf dist && webpack --config webpack.prod.js",
    "build:dev": "rm -rf dist && webpack --config webpack.staging.js"
  },
  "devDependencies": {
    "@fangcha/datawich-frontend": "workspace:*",
    "@web/datawich-common": "workspace:*"
  }
}
